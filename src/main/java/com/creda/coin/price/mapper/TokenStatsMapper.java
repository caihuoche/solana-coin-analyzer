/*
package com.creda.coin.price.mapper;

import java.util.List;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.creda.coin.price.entity.TokenStats;

import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Select;

*/
/**
 * <p>
 * Meme coin 统计数据表，包含 PnL、交易量、价格变化等信息 Mapper 接口
 * </p>
 *
 * @author gain
 * @since 2024-09-11
 *//*

@Mapper
public interface TokenStatsMapper extends BaseMapper<TokenStats> {

	@Select("SELECT * FROM token_stats WHERE granularity = #{granularity} ")
	List<TokenStats> listByGranularity(Integer granularity);
}
*/
